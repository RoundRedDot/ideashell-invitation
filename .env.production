# Production Environment Variables
# This file is for production deployment

# Base path configuration for subpath deployment
# The application is deployed at /user/invite/ on the server
NEXT_PUBLIC_BASE_PATH=/user/invite

# Production site URL for metadata generation
# Update this to your actual domain
NEXT_PUBLIC_SITE_URL=https://ideashell.com

# Sentry configuration (already in .env)
# Add other production-specific variables here